/**
 * app.wxss 是小程序的全局样式文件，用于定义整个小程序通用的样式规则。
 * 这里的样式会作用于所有页面和组件，除非被页面或组件自身的样式覆盖。
 */
.container { /* 定义一个名为 container 的全局样式类，通常用于页面或组件的根容器 */
  height: 100%; /* 设置容器高度为 100%，使其占满父容器的高度 */
  display: flex; /* 启用 Flexbox 布局，使子元素可以灵活排列 */
  flex-direction: column; /* 设置 Flexbox 的主轴方向为垂直，子元素将从上到下排列 */
  align-items: center; /* 在交叉轴（此处为水平方向）上居中对齐子元素 */
  justify-content: space-between; /* 在主轴（此处为垂直方向）上，使子元素之间以及子元素与容器边缘之间保持等距分布 */
  padding: 200rpx 0; /* 设置容器的内边距，上下各 200rpx，左右为 0 */
  box-sizing: border-box; /* 设置盒模型为 border-box，使 padding 和 border 的值包含在元素的 width 和 height 之内 */
}
